<%= form_with(model: todo_user, local: true) do |form| %>
  <% if todo_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(todo_user.errors.count, "error") %> prohibited this todo_user from being saved:</h2>

      <ul>
      <% todo_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :username %>
    <%= form.text_field :username, id: :todo_user_username %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :todo_user_email %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.text_field :password, id: :todo_user_password %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
